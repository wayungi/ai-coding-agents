# Daily

A responsive React + TypeScript todo app. Add tasks, mark them complete, delete them, and filter by status. Tasks are saved locally in your browser.

## Development

```sh
npm install
npm run dev
```

## Production

```sh
npm run build
npm run preview
```
